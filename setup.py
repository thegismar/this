import setuptools

setuptools.setup(
    name="w3helpers",  # Replace with your own username
    version="0.0.5",
    packages=setuptools.find_packages(),
    include_package_data=True,
    python_requires='>=3.9',
)
